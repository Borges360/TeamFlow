# Operational playbooks

Invoke one of `/feature`, `/bugfix`, `/tests`, `/performance`, `/adr`, `/finops`, `/doc`, or `/refactor`. Registry metadata routes to one primary workflow; only the selected playbook is loaded. Repository discovery, workspace, branch, pipeline, documentation routing, gates, and review remain shared policies rather than additional public playbooks.
