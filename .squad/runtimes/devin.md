# Runtime Mapping: Devin

Provide root `AGENTS.md`, the selected workflow, and relevant `.project/` context as repository knowledge/instructions. The active Devin session owns the delivery index and uses its native planning, shell, browser, repository, testing, and review facilities.

Delegate or separate reviewer work only through native capabilities available in the configured environment. Each handoff still follows the task/result contracts.

Do not implement a Devin adapter or simulate Devin agents. When a native feature cannot satisfy a role separation, execute sequentially and record the independence limitation.
