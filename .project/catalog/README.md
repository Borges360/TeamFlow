# EXAMPLE CONTEXT — federated catalog

These files demonstrate the metadata shape for selective discovery. Replace them with real records, split by domain if useful, or connect agents to an authoritative service catalog through native runtime capabilities.

Do not interpret placeholder URLs, owners, dependencies, criticality, or classifications as real. Stable IDs are references; visibility does not grant write authority.
